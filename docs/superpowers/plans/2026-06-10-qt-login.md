# Qt 登录界面 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 构建一个 macOS 原生风格的 Qt Widgets 登录界面，本地验证 `admin`/`123456`，登录结果通过状态标签反馈。

**Architecture:** `AuthValidator` 提供纯静态验证逻辑（可单元测试），`LoginWindow` 负责 UI 布局与交互流程，`main.cpp` 作为应用入口。UI 与验证逻辑分离，CMake 管理构建。

**Tech Stack:** C++17, Qt 6.11 (Widgets + Test), CMake 3.16+

**Spec:** `docs/superpowers/specs/2026-06-10-qt-login-design.md`

---

## 文件结构

| 文件 | 职责 |
|------|------|
| `QtLoginDemo/CMakeLists.txt` | 构建配置，主程序 + 单元测试两个 target |
| `QtLoginDemo/src/AuthValidator.h` | 验证器接口声明 |
| `QtLoginDemo/src/AuthValidator.cpp` | 本地账号比对实现 |
| `QtLoginDemo/src/LoginWindow.h` | 登录窗口类声明 |
| `QtLoginDemo/src/LoginWindow.cpp` | UI 布局 + 登录交互逻辑 |
| `QtLoginDemo/src/main.cpp` | 应用入口 |
| `QtLoginDemo/tests/test_auth_validator.cpp` | AuthValidator 单元测试 |

---

### Task 1: CMake 项目脚手架

**Files:**
- Create: `QtLoginDemo/CMakeLists.txt`

- [ ] **Step 1: 创建 CMakeLists.txt**

```cmake
cmake_minimum_required(VERSION 3.16)
project(QtLoginDemo VERSION 1.0.0 LANGUAGES CXX)

set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_AUTOMOC ON)

find_package(Qt6 REQUIRED COMPONENTS Widgets Test)

# --- 主程序 ---
add_executable(QtLoginDemo
    src/main.cpp
    src/LoginWindow.cpp
    src/AuthValidator.cpp
)
target_link_libraries(QtLoginDemo PRIVATE Qt6::Widgets)
target_include_directories(QtLoginDemo PRIVATE src)

# --- 单元测试 ---
enable_testing()

add_executable(test_auth_validator
    tests/test_auth_validator.cpp
    src/AuthValidator.cpp
)
target_link_libraries(test_auth_validator PRIVATE Qt6::Test Qt6::Widgets)
target_include_directories(test_auth_validator PRIVATE src)

add_test(NAME AuthValidator COMMAND test_auth_validator)
```

- [ ] **Step 2: 验证 CMake 配置能解析**

Run:
```bash
cd QtLoginDemo
cmake -B build -DCMAKE_PREFIX_PATH=/opt/homebrew/opt/qt@6
```
Expected: 配置成功，但链接失败（源文件尚不存在）—— 此时只需确认 CMake 能找到 Qt6 即可。如果因缺少源文件报错，先创建空的 `src/main.cpp`（内容：`int main() { return 0; }`）让配置通过，Task 5 会覆盖。

- [ ] **Step 3: Commit**

```bash
git add QtLoginDemo/CMakeLists.txt
git commit -m "feat: add CMake build scaffold for QtLoginDemo"
```

---

### Task 2: AuthValidator（TDD）

**Files:**
- Create: `QtLoginDemo/src/AuthValidator.h`
- Create: `QtLoginDemo/src/AuthValidator.cpp`
- Create: `QtLoginDemo/tests/test_auth_validator.cpp`

- [ ] **Step 1: 写失败测试**

创建 `QtLoginDemo/tests/test_auth_validator.cpp`：

```cpp
#include <QtTest>
#include "AuthValidator.h"

class TestAuthValidator : public QObject {
    Q_OBJECT

private slots:
    void correctCredentials();
    void wrongPassword();
    void wrongUsername();
    void emptyUsername();
    void emptyPassword();
};

void TestAuthValidator::correctCredentials() {
    QCOMPARE(AuthValidator::validate("admin", "123456"),
             AuthValidator::Result::Success);
}

void TestAuthValidator::wrongPassword() {
    QCOMPARE(AuthValidator::validate("admin", "wrong"),
             AuthValidator::Result::WrongCredentials);
}

void TestAuthValidator::wrongUsername() {
    QCOMPARE(AuthValidator::validate("user", "123456"),
             AuthValidator::Result::WrongCredentials);
}

void TestAuthValidator::emptyUsername() {
    QCOMPARE(AuthValidator::validate("", "123456"),
             AuthValidator::Result::WrongCredentials);
}

void TestAuthValidator::emptyPassword() {
    QCOMPARE(AuthValidator::validate("admin", ""),
             AuthValidator::Result::WrongCredentials);
}

QTEST_MAIN(TestAuthValidator)
#include "test_auth_validator.moc"
```

创建占位头文件 `QtLoginDemo/src/AuthValidator.h`（让编译能找到符号）：

```cpp
#pragma once

#include <QString>

class AuthValidator {
public:
    enum class Result { Success, WrongCredentials };

    static Result validate(const QString& username, const QString& password);
};
```

创建空实现 `QtLoginDemo/src/AuthValidator.cpp`：

```cpp
#include "AuthValidator.h"

AuthValidator::Result AuthValidator::validate(const QString& username,
                                              const QString& password) {
    return Result::WrongCredentials;
}
```

- [ ] **Step 2: 运行测试确认失败**

Run:
```bash
cd QtLoginDemo
cmake -B build -DCMAKE_PREFIX_PATH=/opt/homebrew/opt/qt@6
cmake --build build --target test_auth_validator
./build/test_auth_validator
```
Expected: FAIL — `correctCredentials` 期望 Success 但得到 WrongCredentials

- [ ] **Step 3: 实现最小验证逻辑**

替换 `QtLoginDemo/src/AuthValidator.cpp`：

```cpp
#include "AuthValidator.h"

AuthValidator::Result AuthValidator::validate(const QString& username,
                                              const QString& password) {
    if (username == "admin" && password == "123456") {
        return Result::Success;
    }
    return Result::WrongCredentials;
}
```

- [ ] **Step 4: 运行测试确认通过**

Run:
```bash
cmake --build build --target test_auth_validator
./build/test_auth_validator
```
Expected: PASS — 全部 5 个测试用例通过

- [ ] **Step 5: Commit**

```bash
git add QtLoginDemo/src/AuthValidator.h QtLoginDemo/src/AuthValidator.cpp QtLoginDemo/tests/test_auth_validator.cpp
git commit -m "feat: add AuthValidator with unit tests"
```

---

### Task 3: LoginWindow UI 布局

**Files:**
- Create: `QtLoginDemo/src/LoginWindow.h`
- Create: `QtLoginDemo/src/LoginWindow.cpp`（仅 UI 部分，登录逻辑留空）

- [ ] **Step 1: 创建 LoginWindow 头文件**

`QtLoginDemo/src/LoginWindow.h`：

```cpp
#pragma once

#include <QWidget>

class QLineEdit;
class QPushButton;
class QLabel;

class LoginWindow : public QWidget {
    Q_OBJECT

public:
    explicit LoginWindow(QWidget* parent = nullptr);

private slots:
    void onLoginClicked();

private:
    void setupUi();
    void setStatus(const QString& text, const QColor& color);
    void setLoginEnabled(bool enabled);

    QLineEdit* m_usernameEdit;
    QLineEdit* m_passwordEdit;
    QPushButton* m_loginButton;
    QLabel* m_statusLabel;
};
```

- [ ] **Step 2: 实现 UI 布局（登录逻辑暂为空）**

`QtLoginDemo/src/LoginWindow.cpp`：

```cpp
#include "LoginWindow.h"

#include <QColor>
#include <QFormLayout>
#include <QHBoxLayout>
#include <QLabel>
#include <QLineEdit>
#include <QPushButton>
#include <QScreen>
#include <QVBoxLayout>
#include <QGuiApplication>

LoginWindow::LoginWindow(QWidget* parent)
    : QWidget(parent)
    , m_usernameEdit(nullptr)
    , m_passwordEdit(nullptr)
    , m_loginButton(nullptr)
    , m_statusLabel(nullptr)
{
    setupUi();
}

void LoginWindow::setupUi()
{
    setWindowTitle(QStringLiteral("登录"));
    setFixedSize(360, 200);

    m_usernameEdit = new QLineEdit(this);
    m_usernameEdit->setPlaceholderText(QStringLiteral("请输入用户名"));

    m_passwordEdit = new QLineEdit(this);
    m_passwordEdit->setPlaceholderText(QStringLiteral("请输入密码"));
    m_passwordEdit->setEchoMode(QLineEdit::Password);

    m_loginButton = new QPushButton(QStringLiteral("登录"), this);
    m_loginButton->setAutoDefault(true);
    m_loginButton->setDefault(true);

    m_statusLabel = new QLabel(QStringLiteral("请输入用户名和密码"), this);
    m_statusLabel->setAlignment(Qt::AlignCenter);
    setStatus(QStringLiteral("请输入用户名和密码"), QColor("#888888"));

    auto* formLayout = new QFormLayout;
    formLayout->addRow(QStringLiteral("用户名"), m_usernameEdit);
    formLayout->addRow(QStringLiteral("密码"), m_passwordEdit);

    auto* buttonLayout = new QHBoxLayout;
    buttonLayout->addStretch();
    buttonLayout->addWidget(m_loginButton);
    buttonLayout->addStretch();

    auto* mainLayout = new QVBoxLayout(this);
    mainLayout->addLayout(formLayout);
    mainLayout->addLayout(buttonLayout);
    mainLayout->addWidget(m_statusLabel);

    connect(m_loginButton, &QPushButton::clicked,
            this, &LoginWindow::onLoginClicked);

    // 窗口居中
    if (auto* screen = QGuiApplication::primaryScreen()) {
        QRect screenGeometry = screen->availableGeometry();
        int x = (screenGeometry.width() - width()) / 2;
        int y = (screenGeometry.height() - height()) / 2;
        move(x, y);
    }
}

void LoginWindow::setStatus(const QString& text, const QColor& color)
{
    m_statusLabel->setText(text);
    m_statusLabel->setStyleSheet(
        QStringLiteral("color: %1;").arg(color.name()));
}

void LoginWindow::setLoginEnabled(bool enabled)
{
    m_loginButton->setEnabled(enabled);
    m_loginButton->setText(enabled
        ? QStringLiteral("登录")
        : QStringLiteral("验证中..."));
}

void LoginWindow::onLoginClicked()
{
    // Task 4 实现
}
```

- [ ] **Step 3: 创建临时 main.cpp 验证 UI 可编译运行**

`QtLoginDemo/src/main.cpp`（临时，Task 5 会完善）：

```cpp
#include <QApplication>
#include "LoginWindow.h"

int main(int argc, char* argv[])
{
    QApplication app(argc, argv);
    LoginWindow window;
    window.show();
    return app.exec();
}
```

- [ ] **Step 4: 构建并运行确认 UI 显示**

Run:
```bash
cd QtLoginDemo
cmake --build build
./build/QtLoginDemo
```
Expected: 弹出 360×200 登录窗口，显示中文标签、输入框、按钮和灰色初始提示。点击登录无反应（逻辑未实现）。

- [ ] **Step 5: Commit**

```bash
git add QtLoginDemo/src/LoginWindow.h QtLoginDemo/src/LoginWindow.cpp QtLoginDemo/src/main.cpp
git commit -m "feat: add LoginWindow UI layout"
```

---

### Task 4: LoginWindow 登录交互逻辑

**Files:**
- Modify: `QtLoginDemo/src/LoginWindow.cpp`（实现 `onLoginClicked`）

- [ ] **Step 1: 实现 onLoginClicked 完整逻辑**

在 `QtLoginDemo/src/LoginWindow.cpp` 顶部添加 include：

```cpp
#include "AuthValidator.h"
```

替换 `onLoginClicked` 方法：

```cpp
void LoginWindow::onLoginClicked()
{
    const QString username = m_usernameEdit->text();
    const QString password = m_passwordEdit->text();

    if (username.isEmpty()) {
        setStatus(QStringLiteral("请输入用户名"), QColor("#FF8C00"));
        return;
    }

    if (password.isEmpty()) {
        setStatus(QStringLiteral("请输入密码"), QColor("#FF8C00"));
        return;
    }

    setLoginEnabled(false);

    const auto result = AuthValidator::validate(username, password);

    if (result == AuthValidator::Result::Success) {
        setStatus(QStringLiteral("登录成功！欢迎，admin"), QColor("#228B22"));
    } else {
        setStatus(QStringLiteral("用户名或密码错误"), QColor("#DC143C"));
    }

    setLoginEnabled(true);
}
```

- [ ] **Step 2: 构建并手动验证核心流程**

Run:
```bash
cmake --build build
./build/QtLoginDemo
```

手动验证：
| 操作 | 期望 |
|------|------|
| 输入 admin / 123456，点击登录 | 绿色「登录成功！欢迎，admin」 |
| 输入 admin / wrong，点击登录 | 红色「用户名或密码错误」 |
| 用户名为空，点击登录 | 橙色「请输入用户名」 |
| 密码为空，点击登录 | 橙色「请输入密码」 |
| 输入正确凭证，按 Enter | 与点击登录相同 |
| 验证中快速连点 | 按钮禁用，不会重复触发 |

- [ ] **Step 3: Commit**

```bash
git add QtLoginDemo/src/LoginWindow.cpp
git commit -m "feat: implement login validation flow in LoginWindow"
```

---

### Task 5: 完善应用入口

**Files:**
- Modify: `QtLoginDemo/src/main.cpp`

- [ ] **Step 1: 完善 main.cpp**

替换 `QtLoginDemo/src/main.cpp` 为最终版本：

```cpp
#include <QApplication>
#include "LoginWindow.h"

int main(int argc, char* argv[])
{
    QApplication app(argc, argv);

    // 不调用 setStyle()，macOS 自动使用原生控件风格

    LoginWindow window;
    window.show();

    return app.exec();
}
```

- [ ] **Step 2: 构建运行确认**

Run:
```bash
cmake --build build
./build/QtLoginDemo
```
Expected: 应用正常启动，控件呈现 macOS 原生外观

- [ ] **Step 3: Commit**

```bash
git add QtLoginDemo/src/main.cpp
git commit -m "feat: add application entry point"
```

---

### Task 6: 全量验证

**Files:** 无新增

- [ ] **Step 1: 运行全部单元测试**

Run:
```bash
cd QtLoginDemo
ctest --test-dir build --output-on-failure
```
Expected: `AuthValidator` 测试 PASS

- [ ] **Step 2: 执行完整手动验证清单**

对照 spec 中的手动验证清单逐项测试：

| # | 场景 | 输入 | 期望 |
|---|------|------|------|
| 1 | 正确登录 | admin / 123456 | 绿色成功提示 |
| 2 | 错误密码 | admin / wrong | 红色错误提示 |
| 3 | 错误用户名 | user / 123456 | 红色错误提示 |
| 4 | 空用户名 | （空）/ 123456 | 橙色「请输入用户名」 |
| 5 | 空密码 | admin / （空） | 橙色「请输入密码」 |
| 6 | Enter 键 | admin / 123456 + Enter | 与点击按钮相同 |

- [ ] **Step 3: Commit（如有修复）**

如果验证中发现问题并修复，单独 commit：
```bash
git add -A
git commit -m "fix: address issues found during manual verification"
```

如果没有修复，跳过此步。

---

## Spec 覆盖检查

| Spec 要求 | 对应 Task |
|-----------|-----------|
| Qt Widgets + C++ | Task 1–5 |
| CMake 构建 | Task 1 |
| AuthValidator 静态验证 admin/123456 | Task 2 |
| 中文 UI 标签和提示 | Task 3, 4 |
| 360×200 固定窗口，居中 | Task 3 |
| 密码掩码、Enter 登录 | Task 3, 4 |
| 验证中按钮禁用 | Task 4 |
| 状态标签颜色反馈 | Task 3, 4 |
| macOS 原生风格（不 setStyle） | Task 5 |
| 登录成功窗口保持打开 | Task 4（无 close 逻辑） |
| 单元测试 | Task 2, 6 |
| 手动验证清单 | Task 6 |
